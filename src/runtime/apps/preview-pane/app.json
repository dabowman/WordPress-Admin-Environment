{
  "$schema": "../../../../docs/schemas/admin-app-v2.json",
  "id": "core:preview-pane",
  "version": 1,
  "title": "Preview pane",
  "description": "Reads the active route's matched entity from `core-data` and renders a live preview alongside the primary region.",
  "role": "complementary",
  "config-schema": {
    "type": "object",
    "properties": {
      "follow": { "type": "string", "default": "content.selection" }
    },
    "additionalProperties": false
  },
  "script": "wp-admin-shell"
}
