import { __experimentalHeading as Heading } from '@wordpress/components';

export default function MediaApp() {
	return (
		<div className="wp-admin-shell-app-media">
			<Heading level={ 2 }>Media</Heading>
			<p>Media library coming in Step 5.</p>
		</div>
	);
}
